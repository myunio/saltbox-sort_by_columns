# frozen_string_literal: true

# This file is left intentionally minimal because the schema is defined dynamically
# in spec/rails_helper for the Combustion test application.

ActiveRecord::Schema.define(version: 0) do
  # Schema is defined in spec/rails_helper
end
